{
  "name": "scpf-frontend",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "axios": "^1.13.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "node scripts/start-with-server.js",
    "dev:frontend-only": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:desktop:backend": "cd .. && cargo build --release -p scpf-server -p scpf-cli",
    "desktop:dev": "cargo tauri dev",
    "desktop:build": "cargo tauri build",
    "desktop:install-shortcut": "sh ../scripts/install_desktop_shortcut.sh"
  }
}
